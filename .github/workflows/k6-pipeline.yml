name: k6 Tests

on:
  push:
    branches:
      - release
  pull_request:
    branches:
      - release

jobs:
  performance-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Run k6 tests
        run: |
          k6 run index.js